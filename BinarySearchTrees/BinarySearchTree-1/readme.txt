Questo programma legge un testo da file e memorizza le parole in un albero di ricerca binaria.
Per tale motivo quando i dati verrano stampati le paole risulteranno essere in ordine alfabetico.

This program reads a text from a file and stores words in a binary search treee so the words will be in alphabetic order.